import { roadMap } from "../assets";

const RoadMap = () => {
  return (
    <section className="relative w-full">
      <img src={roadMap} alt="roadmap" className="w-full h-auto object-cover" />
    </section>
  );
};

export default RoadMap;
